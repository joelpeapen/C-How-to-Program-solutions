// get 10 positive ints and find largest. Use counter, number, largest
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int counter = 0, number = 0, largest = 0;
    printf("%s", "Enter a number:  ");
    scanf("%d", &number);

    return 0;
}